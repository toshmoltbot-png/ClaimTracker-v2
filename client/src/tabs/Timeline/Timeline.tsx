import { PlaceholderTab } from '@/tabs/shared'

export function Timeline() {
  return <PlaceholderTab summary="Timeline synthesis will attach to the typed data model in a later phase." title="Timeline" />
}
